using Microsoft.AspNetCore.Identity;

namespace AuthManagerWebApi1.Identity;

/// <summary>Your app's user. Add custom profile fields here — AuthManager works with whatever you add.</summary>
public sealed class ApplicationUser : IdentityUser
{
}
