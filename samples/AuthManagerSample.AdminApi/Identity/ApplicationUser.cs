using Microsoft.AspNetCore.Identity;

namespace AuthManagerSample.AdminApi.Identity;

/// <summary>Minimal application user — this sample only needs what ASP.NET Identity provides.</summary>
public sealed class ApplicationUser : IdentityUser
{
}
