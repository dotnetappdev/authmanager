@* Marker component — having at least one local .razor file makes the SDK's Razor
   Components build targets activate for this project, which is what generates
   the /_framework/blazor.web.js static web asset entry in its own manifest.
   AuthManager itself is served entirely from the AuthManager.UI RCL; this file
   has no other purpose. *@
