@* Minimal file — triggers the Blazor SDK targets so that _framework/blazor.web.js
   is included in the static-web-assets manifest even though this host app has no
   Razor pages of its own. AuthManager supplies all the UI via its own RCL. *@
